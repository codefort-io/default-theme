{% extends 'layout/theme' %}

{% block content %}
<section class="container mx-auto px-4 pt-8 pb-12 sm:py-12">
    <h1 class="text-3xl font-bold text-center">
        Checkout
    </h1>
    <div class="mt-4 text-center">
    	<p>
			No checkouts available.
        </p>
        <a href="/cart" class="mt-8 w-full sm:w-auto inline-block uppercase bg-black hover:bg-gray-800 py-3 sm:py-2 px-4 text-white text-sm font-medium rounded-sm">
            {{ 'cart.general.continue_shopping' | t }}  {{ 'fal fa-long-arrow-right' | fa: 'fa-lg ml-2' }}
        </a>
    </div>
</section>
{% endblock %}